package instalacion;

import co.com.sofka.domain.generic.Entity;
import instalacion.values.InversionId;

public class Inversion extends Entity<InversionId> {
     public Inversion(InversionId entityId) {
        super(entityId);
    }
}
